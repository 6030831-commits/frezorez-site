# Задача для Claude Code: редизайн страницы /shezlongi-iz-dereva

Переверстать существующую товарную страницу деревянных шезлонгов по новому прототипу.
Дизайн утверждён — не предлагай альтернативы, реализуй.

## Источники (прочитать перед началом)
- **Прототип = эталон структуры и вёрстки:** `design/shezlongi-iz-dereva.html` (в корне репо).
- **DESIGN.md** — единственный источник правды по токенам (цвета, отступы, радиусы, типографика).
  Прототип уже ему следует; сверяйся с ним, хардкод hex запрещён — только CSS-переменные/токены.
- **semantics.md** (кластер К1) — ключи, которые нельзя терять в тексте и мета.
- Шрифты **Unbounded** (заголовки) + **Manrope** (текст) уже подключены в проекте. Новые не добавлять.

## Жёсткие требования — SEO, НЕ сломать
1. URL/слаг `/shezlongi-iz-dereva` оставить как есть. `build.format:'file'`, без завершающего слэша.
2. `src/layouts/Base.astro` НЕ трогать: там canonical, OG, Яндекс-верификация и счётчик
   Метрики 53352265. Страница должна и дальше рендериться внутри Base.
3. Title/description сохранить или улучшить, ключи из semantics.md (К1) не вырезать.
4. **Никаких форм и сбора ПДн.** Конверсия — только прямые контакты из `src/data/site.js`:
   Telegram `t.me/frezorezwood`, WhatsApp `wa.me/79255560166`, тел. `tel:+79255560166`.

## Архитектура
- Это маркетинговый лендинг с секциями (hero → УТП → модели → брендирование → материалы →
  применения → процесс → цена → FAQ → CTA → перелинковка). Он НЕ влезает в текстовый шаблон
  `pages.js` / `[slug].astro`.
- Реализовать как отдельную страницу `src/pages/shezlongi-iz-dereva.astro` (секции при желании
  вынести в `src/components/shezlongi/*`). URL при этом не меняется.
- Картинки — из `public/foto/shezlongi/` по именам из прототипа: `hero.jpg`, `model-classic.jpg`,
  `model-folding.jpg`, `model-premium.jpg`, `branding-macro.jpg`. Если файла пока нет — оставить
  аккуратный плейсхолдер (как в прототипе), а не битый `<img>`.

## Плейсхолдеры данных — не выдумывать
Сохранить видимыми пометки `[НУЖНА ЦИФРА]` (габариты, вес, сроки, цена партий 20/50/100).
Реальные цифры владелец вставит позже. Ничего не сочинять.

## Перелинковка
Блок «смотрите также»: `/skladnaya-mebel`, `/mebel-dlya-meropriyatij`, `/pressvoly`.

## Schema.org
Добавить на страницу JSON-LD `Product` или `Service`: название, brand FREZOREZ,
areaServed Москва/МО. `offers` с `"от"` — только если появится реальная цифра, иначе без цены.

## Проверка перед коммитом
- [ ] `npm run build` проходит без ошибок.
- [ ] `/shezlongi-iz-dereva` отдаёт 200, URL без слэша.
- [ ] Остальные страницы не задеты (диффом проверить, что менялась только эта страница + новые компоненты).
- [ ] Адаптив: на мобильной ширине hero и сетки моделей/применений схлопываются в 1 колонку.
